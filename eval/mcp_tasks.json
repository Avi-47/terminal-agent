[
  {
    "task_id": "symbol_analysis_validate",
    "description": "Find where validate_workspace is defined and identify its references.",
    "setup": {
      "files": {
        "validator.py": "def validate_workspace(workspace, stress_config=None):\n    return True\n",
        "agent.py": "from validator import validate_workspace\n\ndef run_agent(workspace):\n    return validate_workspace(workspace)\n",
        "tests/test_validator.py": "from validator import validate_workspace\n\ndef test_validation():\n    assert validate_workspace(None)\n"
      }
    },
    "success_condition": {
      "type": "response_contains",
      "text": "validator.py"
    }
  },
  {
    "task_id": "symbol_analysis_run_agent",
    "description": "Find where run_agent is defined and identify its references.",
    "setup": {
      "files": {
        "agent.py": "def run_agent(workspace):\n    return workspace\n",
        "runner.py": "from agent import run_agent\n\ndef main(workspace):\n    return run_agent(workspace)\n"
      }
    },
    "success_condition": {
      "type": "response_contains",
      "text": "agent.py"
    }
  }
]